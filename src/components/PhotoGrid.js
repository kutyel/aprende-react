import React from 'react'

// TODO: importar el componente `Photo`

const PhotoGrid = props => (
  <div className='photo-grid'>
    {/**
     * TODO: renderizar aquí el array de fotos a partir de un JSON
     *
     * Pistas:
     * - el array de datos está en `props.posts`
     * - hay que transformar el array de JS en un array de `Photo`
     * - el componente `Photo` *requiere*: `key`, `i` (índice), `post`, y `{...props}`
     */}
  </div>
)

export default PhotoGrid
